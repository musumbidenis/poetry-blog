class Post {
  final String postId;
  final String title;
  final String description;
  final String imageUrl;
  final String username;
  final String created_at;

  Post(this.postId, this.title, this.description, this.imageUrl, this.username, this.created_at);
}